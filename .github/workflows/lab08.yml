name: Lab 08 - Python
on:
  push:
    paths:
      - 'lab08/**'
  pull_request:
    paths:
      - 'lab08/**'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      - name: Install dependencies
        run: pip install -r requirements.txt
      - name: Run Lab 08 tests
        run: pytest lab08/tests/ -v
