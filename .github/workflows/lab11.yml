name: Lab 11 - Rust
on:
  push:
    paths:
      - 'lab11/**'
  pull_request:
    paths:
      - 'lab11/**'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: dtolnay/rust-toolchain@stable
      - name: Run cargo test
        run: cd lab11 && cargo test --verbose
      - name: Run cargo fmt check
        run: cd lab11 && cargo fmt --check
      - name: Run cargo clippy
        run: cd lab11 && cargo clippy -- -D warnings
