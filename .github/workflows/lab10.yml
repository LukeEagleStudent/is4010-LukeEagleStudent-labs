name: Lab 10 - Rust
on:
  push:
    paths:
      - 'lab10/**'
  pull_request:
    paths:
      - 'lab10/**'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: dtolnay/rust-toolchain@stable
      - name: Run cargo test
        run: cd lab10 && cargo test --verbose
      - name: Run cargo fmt check
        run: cd lab10 && cargo fmt --check
      - name: Run cargo clippy
        run: cd lab10 && cargo clippy -- -D warnings
